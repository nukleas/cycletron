//! Pattern validation and analysis, re-exported from the shared
//! `robostrudel-analysis` crate so CLI tools (`tools/song-check`) run the
//! exact same pipeline as the in-app agent. See that crate for the
//! implementation and its tests.

pub use robostrudel_analysis::*;
