# Local MusicRepo (works + representations). Homebrew Postgres on this machine,
# loaded from strulibrary/db/corpus.jsonl — not the paused Supabase project.
# Recreate / reload:
#   python3 ~/Code/strulibrary/db/load.py
DATABASE_URL=postgresql:///musicrepo
