// dark ambient in C phrygian (generated) — beatless low drone, slow dark pads, distant bells
setbpm(55);

stack(
  note("c1").s("sine").attack(4).release(6).gain(0.5),
  note("<[c3, d#3, g3, a#3] [c#3, f3, g#3, c4]>").s("wt_strings").attack(3).release(5).lpf(700).gain(0.3).room(0.9).pan(0.42),
  note("c4 ~ ~ c4 ~ ~ g3 ~").s("wt_bell").room(0.85).delay(0.5).gain(0.26).pan(0.62)
)
