// gabber in A phrygian (generated) — distorted gabber kick wall, driving 16th bass
setbpm(180);

stack(
  s("bd ~ ~ ~ bd ~ ~ ~ bd ~ ~ ~ bd ~ bd ~, ~ hh ~ hh ~ hh ~ hh ~ hh ~ hh ~ hh ~ hh").gain(0.95).dist(0.5),
  note("a2 ~ a2 ~ a2 ~ a2 ~ a2 ~ a2 a#2 a2 ~ a2 ~").s("sawtooth").lpf(900).resonance(8).gain(0.48).dist(0.2),
  note("<[a3, c4, e4, g4] [a#3, d4, f4, a4]>").s("sawtooth").struct("~ ~ 1 ~").release(0.1).lpf(1800).gain(0.3).pan(0.42)
)
