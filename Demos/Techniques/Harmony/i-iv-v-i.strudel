// Classic I–IV–V–I in C major, one chord per cycle (slowcat).
// `chord(...)` expands each symbol into its constituent notes.
setbpm(90);

chord("<C F G C>")
  .voicing()
  .s("sawtooth")
  .cutoff(1200)
  .attack(0.05)
  .release(0.6)
  .gain(0.55)
