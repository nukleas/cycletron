// Call-and-response via `superimpose`: the response layer is the call
// transposed up an octave and shifted later in time.
setbpm(104);

note("c4 e4 g4 b4 ~ ~ ~ ~")
  .s("sine")
  .superimpose(x => x.transpose(12).late(0.5))
  .attack(0.01)
  .release(0.4)
  .gain(0.5)
