// Continuous stereo motion: a sine LFO sweeps the pan across 0.2..0.8
// (never negative — negative pan is silent). The lead drifts side to side.
setbpm(120);

stack(
  s("bd*4"),
  note("0 3 7 10 7 3").scale("c4:minor").s("wt_lead")
    .pan(sine.range(0.2, 0.8))
    .room(0.3).gain(0.5)
)
