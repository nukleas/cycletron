[package]
name = "cycletron-corpus"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[dependencies]
# Single-pass multi-needle scan for curated-metadata extraction. Already in
# Cargo.lock transitively — zero new compiled dependencies.
aho-corasick = "1"
cycletron-core = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
anyhow = { workspace = true }
tracing = { workspace = true }
walkdir = "2"

[dev-dependencies]
criterion = { workspace = true }

[[bench]]
name = "corpus"
harness = false
