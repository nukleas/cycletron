// Supersaw lead: detuned saw stack with a resonant lowpass.
// `detune(30)` widens the stereo image; lower it for tighter sounds.
setbpm(126);

note("e4 g4 b4 d5 b4 g4")
  .s("supersaw")
  .detune(30)
  .cutoff(2400)
  .resonance(0.4)
  .attack(0.05)
  .release(0.4)
  .gain(0.5)
