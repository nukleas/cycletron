// FM bass: 2-op synthesis with a moving modulation index.
// `range(0.5, 6)` maps a sine LFO into a useful fmindex range.
setbpm(112);

note("c2 c2 eb2 g2")
  .s("fm")
  .fmindex(sine.range(0.5, 6))
  .fmratio(2)
  .attack(0.01)
  .release(0.35)
  .gain(0.65)
