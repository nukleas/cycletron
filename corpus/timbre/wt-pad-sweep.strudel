// Wavetable pad with slow LFO filter sweep and long sustain envelope.
setbpm(90);

note("<c3 ab3 bb3 g3>")
  .s("wt_pad")
  .slow(2)
  .cutoff(sine.slow(16).range(300, 2200))
  .resonance(4)
  .attack(1.2)
  .release(2.5)
  .gain(0.38)
  .room(0.6)
  .roomsize(0.8)
