// D Dorian: minor scale with a raised 6th (B natural).
// `slowcat` alternates an ascending phrase with its descent — total 2 cycles.
setbpm(100);

slowcat(
  note("d4 e4 f4 g4 a4 b4 c5 d5"),
  note("d5 c5 b4 a4 g4 f4 e4 d4")
)
  .s("triangle")
  .cutoff(1800)
  .release(0.25)
  .gain(0.55)
